{
    "caption": "Search text",
    "fields": {
        "ACR": {
            "caption": "Term",
            "name": "term",
            "value": "text",
            "rank": 0,
            "editor": "DynamicValue"
        }
    },
    "code": {
        "fn": "function(instance, properties, context) {\n    var pv = instance.data && instance.data.pv;\n    if (!pv) return;\n    pv.search(properties.term);\n}"
    }
}
