{
    "caption": "Fit to page",
    "name": "fit_page",
    "fields": {},
    "code": {
        "fn": "function(instance, properties, context) {\n    var pv = instance.data && instance.data.pv;\n    if (!pv) return;\n    pv.fitPage();\n}"
    }
}
